rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null &&
        request.auth.token.email in ['ofirmor1234@gmail.com', 'mankal@hovlim.com', 'noya.haasnoot@gmail.com'];
    }
  }
}
